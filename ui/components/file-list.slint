import { Colors } from "../theme/colors.slint";

export struct UiFileItem {
    icon: string,
    name: string,
    path: string,
    file_type: string,
    size: string,
    modified: string,
}

export component FileList inherits Rectangle {
    in property <[UiFileItem]> files;
    in-out property <int> selected_index: -1;

    callback open_folder(path: string);
    callback open_file(path: string);
    callback select_file(path: string);

    background: Colors.content;

    VerticalLayout {
        spacing: 4px;

        Rectangle {
            height: 40px;
            background: Colors.toolbar;
            border-radius: 10px;

            HorizontalLayout {
                spacing: 0;
                padding-left: 12px;
                padding-right: 12px;

                Text {
                    text: "Name";
                    color: Colors.text;
                    width: 420px;
                    vertical-alignment: center;
                    font-weight: 600;
                }

                Text {
                    text: "Type";
                    color: Colors.text;
                    width: 170px;
                    vertical-alignment: center;
                    font-weight: 600;
                }

                Text {
                    text: "Size";
                    color: Colors.text;
                    width: 120px;
                    vertical-alignment: center;
                    font-weight: 600;
                }

                Text {
                    text: "Modified";
                    color: Colors.text;
                    width: 190px;
                    vertical-alignment: center;
                    font-weight: 600;
                }
            }
        }

        if (root.files.length == 0) : Rectangle {
            background: Colors.content;
            border-radius: 12px;
            border-width: 1px;
            border-color: #1f2a3a;

            VerticalLayout {
                alignment: center;
                spacing: 10px;

                Text {
                    text: "📁";
                    color: Colors.text;
                    font-size: 28px;
                    horizontal-alignment: center;
                }

                Text {
                    text: "This folder is empty";
                    color: Colors.text;
                    horizontal-alignment: center;
                    font-weight: 600;
                }

                Text {
                    text: "Create a folder or copy files here";
                    color: Colors.text_secondary;
                    horizontal-alignment: center;
                }
            }
        }

        for file[i] in root.files : Rectangle {
            height: 46px;
            background: i == root.selected_index
                ? #17304a
                : (touch.has-hover ? #101d2d : Colors.content);
            border-width: 1px;
            border-color: i == root.selected_index ? #3b82f6 : #00000000;
            border-radius: 10px;

            touch := TouchArea {
                clicked => {
                    root.selected_index = i;
                    root.select_file(file.path);
                }

                double-clicked => {
                    if (file.file_type == "Directory") {
                        root.open_folder(file.path);
                    } else {
                        root.open_file(file.path);
                    }
                }
            }

            HorizontalLayout {
                spacing: 0;
                padding-left: 12px;
                padding-right: 12px;

                Rectangle {
                    width: 44px;
                    height: parent.height;
                    background: #00000000;

                    Text {
                        text: file.icon;
                        color: Colors.text;
                        horizontal-alignment: center;
                        vertical-alignment: center;
                    }
                }

                Text {
                    text: file.name;
                    color: Colors.text;
                    width: 376px;
                    vertical-alignment: center;
                    font-weight: 600;
                }

                Text {
                    text: file.file_type;
                    color: Colors.text;
                    width: 170px;
                    vertical-alignment: center;
                }

                Text {
                    text: file.size;
                    color: Colors.text;
                    width: 120px;
                    vertical-alignment: center;
                }

                Text {
                    text: file.modified;
                    color: Colors.text;
                    width: 190px;
                    vertical-alignment: center;
                }
            }
        }

        Rectangle {
            background: Colors.content;
        }
    }
}
